import 'package:flutter/cupertino.dart';

class Notificaton extends StatelessWidget {
  const Notificaton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child:Text("notification"),
    );
  }
}
